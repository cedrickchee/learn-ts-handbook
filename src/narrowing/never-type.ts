/**
 * The `never` type
 */

// When narrowing, you can reduce the options of a union to a point where you
// have removed all possibilities and have nothing left.
// In those cases, TypeScript will use a never type to represent a
// state which shouldn’t exist.

export {};
